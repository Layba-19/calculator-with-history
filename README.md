# calculator-with-history
A simple Python-based command-line calculator that supports basic arithmetic operations, power (^) operator, and maintains a calculation history using file handling. Includes input validation and error handling for reliable user experience.
